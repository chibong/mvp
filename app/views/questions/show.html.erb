<h1>Feedback Results </h1>

<p>
  <b>Your feedback for the question: "</b><%= @question.summary %>"
</p>

  <br>
  <table class="table table-hover table-bordered table-striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Rating</th>
    </tr>
  </thead>
  <tbody>
    <tr>
       <% @question.answers.each do |answer| %>
      <tr>
      <td><%= answer.created_at.strftime("%b %d")%></td>
      <td><%= answer.number %></td>
      </tr>
      <% end %>

  </tbody>
</table>



